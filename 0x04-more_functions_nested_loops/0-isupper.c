#include "main.h"
/**
 * a function that checks for uppercase 
 * upper: return 1
 * lower: return 0
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
